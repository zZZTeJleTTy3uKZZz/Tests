import re

with open('Agents.md', 'r', encoding='utf-8') as f:
    text = f.read()

old_solution = """- **Решение:** 
  > **⚠️ ОШИБКА / НЕ РАБОТАЕТ:** Первоначальная попытка обмануть React через создание нового элемента (`insertBefore`) и скрытие старого (`display: none`) провалилась на масштабе. React всё равно синхронизирует состояние глобально или затирает DOM при навигации.
  > *Статус: Проблема в процессе решения, завтра будут протестированы новые подходы (возможно, перехват данных через API или полная изоляция).*"""

new_solution = """- **Решение:** Нельзя использовать замену существующих узлов (`replaceWith`), так как React откатывает изменения. Кроме того, в Next.js клик по табу (например, `npm`) переключает состояние **глобально по всей странице**. 
  
  **Рабочий алгоритм:**
  1. Сначала собираем список всех уникальных названий табов на странице (со всех блоков кода).
  2. В цикле кликаем по каждому уникальному имени таба с помощью **встроенного метода Playwright** (`page.get_by_role("tab", name=...).click()`), а не через JS `evaluate`, так как JS-клик может игнорироваться синтетическими событиями React.
  3. Делаем паузу `wait_for_timeout` для отработки React Hydration.
  4. Собираем тексты всех блоков кода с активными на данный момент табами и сохраняем их в память скрипта (в Python словарь).
  5. В самом конце, одним JS-скриптом генерируем блоки с метками `MAGICSTART Имя таба MAGICEND` и вставляем их в DOM перед оригинальными блоками (с помощью `insertBefore`).
  6. На этапе конвертации (после `html2text`) заменяем метки `MAGIC` на Markdown заголовки `### [Имя таба]`. Это гарантированно сохраняет форматирование и предотвращает потерю данных."""

text = text.replace(old_solution, new_solution)

old_feature = """- **Особенности:** Документация содержит интерактивные блоки кода (табы pnpm/npm). 
  > **⚠️ ВАЖНО (Статус):** Трюк с обходом React Hydration (создание нового элемента через `insertBefore` и скрытие старого `display: none`) **НЕ СРАБОТАЛ** на практике при массовом парсинге. Фреймворк всё равно перехватывает состояние, либо вкладки выгружаются некорректно. Завтра мы будем тестировать другие варианты решения этой проблемы."""

new_feature = """- **Особенности:** Документация содержит интерактивные блоки кода (табы pnpm/npm, JS/TS). В Next.js табы синхронизируются глобально (переключение языка в одном блоке переключает язык на всей странице).
  - Реализован паттерн "Сбор в память + Финальная инъекция", обходящий защиту React Virtual DOM.
  - Используются "грязные" метки (MAGICSTART/MAGICEND) для безопасного проноса названий табов через конвертер `html2text`."""

text = text.replace(old_feature, new_feature)

with open('Agents.md', 'w', encoding='utf-8') as f:
    f.write(text)

